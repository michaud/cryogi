import React from 'react';

const Intro = () => {
    return (
        <div className="c-box--intro">
            <h2 className="first">Introduction</h2>
            <p>Add a portfolio, select portfolio, add a project, add the project to the portfolio, save the portfolio</p>
        </div>
    );
};

export default Intro;
